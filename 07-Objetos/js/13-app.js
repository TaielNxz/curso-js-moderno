const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 30,
    disponible: true,
}

// te retorna las llaves del producto
console.log( Object.keys( producto ) );

// te retorna los valores del objeto
console.log( Object.values( producto ) );

// te retorna ambos en pares
console.log( Object.values( producto ) );