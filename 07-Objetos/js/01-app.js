const nombreProducto = "Monitor 20 Pulgadas";
const precio = 30;
const disponible = true;

// crear un objeto ( Object Literal )
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 30,
    disponible: true,
}

console.log(producto);