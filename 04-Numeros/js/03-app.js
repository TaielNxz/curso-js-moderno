let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);  // 3
resultado = Math.round(2.2);  // 2
resultado = Math.round(2.6);  // 3
resultado = Math.round(2.5);  // 3
resultado = Math.round(2.4);  // 2

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.9);

// Raiz Cuadrada
resultado = Math.sqrt(144);

// Absoluto
resultado = Math.abs(-500);

// Potencia
resultado = Math.pow(2, 4);

// Minimo
resultado = Math.min(3, 5, 12, -3);

// Maximo
resultado = Math.max(3, 5, 12, -3);

// Aleatorio
resultado = Math.random();

// Aleatorio dentro de un rango...
resultado = Math.floor( Math.random() * 30 );