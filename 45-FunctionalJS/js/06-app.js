// Pure Functions
/*
    no modifican el valor original
    si toma una entrada debe retornar una sola salida
*/

const numero1 = 20;
const duplicar = numero => numero * 2;


const resultado = duplicar(numero1);

console.log(numero1);
console.log(resultado);
